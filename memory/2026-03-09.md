# 2026-03-09

- Reviewed GMX aggregate DMARC report for `freeland.rocks` covering 2026-03-08 UTC.
- GMX saw 1 message from `185.26.156.114` (`mailgate02.uberspace.is`) with SPF pass and DKIM pass.
- Live DNS check matched the report:
  - `_dmarc.freeland.rocks`: `v=DMARC1; p=none; rua=mailto:postmaster@freeland.rocks`
  - `freeland.rocks`: `v=spf1 include:spf.uberspace.de ~all`
  - `uberspace._domainkey.freeland.rocks`: DKIM key present
- Current DMARC policy is monitoring-only (`p=none`).
- Upgraded Raspberry Pi `openclaw-gateway` from `v2026.3.2` to `v2026.3.8` via `npx --yes openclaw@2026.3.8 gateway install --force --port 18789 --token "$TOKEN"`.
- Verified the regenerated systemd unit reports `OpenClaw Gateway (v2026.3.8)` and `openclaw gateway health` returns `OK`.
- Updated repo references to `openclaw@^2026.3.8` in `package.json`, `package-lock.json`, and the Pi/Uberspace runbook.
- Hardened `apps/web-next` against Prisma SQLite outages after reproducing homepage 500s when the parcel DB file is missing/unopenable.
- Homepage now degrades gracefully with a database-unavailable banner, parcel APIs return controlled `503` JSON, and parcel detail pages show a fallback state instead of crashing the whole site.
