# 2026-03-12

- Reworked the Raspberry Pi OpenClaw dashboard GUI in `scripts/monitor/openclaw-dash-gui.py`.
- Header is now a canvas-drawn title row with fixed `LINES` count so it renders reliably on the TFT.
- Log viewport now forces bottom-follow when new lines arrive.
- Verified on the Pi over SSH that appending a probe line to `/tmp/openclaw/openclaw-2026-03-12.log` updates the dashboard automatically without touching the screen.
- Iterated the compact-screen palette and typography to move the display back toward the original cyan-on-black look.
- Audited launch readiness against the live site (`https://freeland.rocks/`) and repo state.
- Live webspace is online and serving the Next app; `/`, `/portfolios`, `/movement`, `/soul`, and `/voting` respond publicly.
- Public issues found: movement page exposes raw ask-failure/log data, voting page is hard-coded mock data, soul page is brochure-only, live site links the main Telegram chat but not the existing `@RobinHoodCoinBot`.
- Product gaps found: no Hugging Face stamp image pipeline, no public email capture/waitlist, no real on-chain stamp purchase flow, governance vote logic still lacks token-holder verification, and transparency report totals are placeholder zeros.
- Runtime signal from live portfolio page: land scout last run failed; page reported 33 scout failures in the last 24h and zero new additions on the latest run.
- Added `LAUNCH_TODO.md` to track launch blockers and progress inside the repo.
- Implemented public waitlist capture in the Next app (`/api/waitlist`, homepage form, JSONL storage).
- Hardened the public site: movement page no longer exposes raw internal logs, voting page now reflects real proposal files or an honest empty state, and public bot links are visible across homepage and soul page.
- Added operator-side launch tooling: Hugging Face stamp image generator, campaign copy generator, waitlist email broadcast tool, and Sherwood opportunity assignment via `/assignopp`.
- Tightened governance/reporting realism: proposal votes now block repeat voter IDs, transparency reports compute tracked totals from available ledgers instead of placeholder zeros, and the land-scout test suite now passes again after fixing the fs mock interaction with dotenv.
- Added member wallet linking, token-holder vote verification, campaign metadata generation, and a real mint-to-wallet path for `/stampmint` when collection info and wallet setup are present.
- Updated the launch backlog so all repo-side TODOs are now marked done; deployment to the live webspace remains separate and was not run.
- Checked Raspberry Pi runtime logs over SSH at `pi@raspberrypi.local` (`192.168.1.17`) after user reported many IP-related errors.
- `openclaw-gateway.service` and `openclaw-tunnel.service` are both active on the Pi; no separate `robinhoodcoin-bot.service` is installed there.
- Main Pi error source is outbound scouting/fetching, not inbound abuse: `~/logs/openclaw-gateway.err.log` shows repeated `403` blocks from Akamai-protected listing domains (`landwatch.com`, `land.com`, `landandfarm.com`) plus `propiedades.com`.
- Verified from the Pi public IP (`77.11.3.225`) that those domains return immediate `403 Forbidden` even with browser-like user-agent headers over both HTTP and HTTPS.
- OpenClaw browser fallback is currently disabled again on the Pi (`~/.openclaw/openclaw.json` has `browser.enabled=false`), and the gateway log shows repeated `Browser control is disabled` errors on March 12.
- Reverse tunnel issue was real but historical: `~/logs/openclaw-tunnel.err.log` is full of `remote port forwarding failed for listen port 18789`; `openclaw-tunnel.service` needed 699 restart attempts before stabilizing at `2026-03-12 04:19 GMT`. Remote port `127.0.0.1:18789` on Uberspace is reachable now.
- No evidence of hostile inbound IP traffic in the Pi service logs; the only raw IP in gateway stderr was scraped page content (`clientIp`) embedded in fetched HTML, not a server error source.
- Full log sweep on the Pi: `~/logs/openclaw-gateway.err.log` had 4,410 lines and `~/logs/openclaw-gateway.log` had 30,382 lines on March 12.
- Top gateway stderr signatures on the Pi were: `274` embedded run timeouts, `102` web `403` fetch failures, `87` browser-disabled errors, `10` missing-file reads, `6` API rate-limit errors, and `1` transient DNS `EAI_AGAIN`.
- Recent missing-file errors are from `~/.openclaw/workspace-governance` trying to read `MISSION.md`, `CONSTRAINTS.md`, and `memory/latest-governance.md`; the workspace exists but those files do not.
- `~/.openclaw/cron/jobs.json` currently contains no jobs, so the recurring timeout cadence is likely driven by an external client or another service hitting the Pi gateway rather than OpenClaw cron on the Pi.
- Patched repo scout defaults in `src/soul/skills/autonomous-land-scout.ts` so default avoided source hosts now include `landandfarm.com` and `propiedades.com`; updated `src/soul/skills/autonomous-land-scout.test.ts` accordingly and the targeted Vitest file passes.
- Fixed Pi OpenClaw runtime config in `~/.openclaw/openclaw.json`: `browser.enabled=true` again and `agents.defaults.timeoutSeconds=420`; restarted `openclaw-gateway.service`.
- Confirmed after restart that browser control is live again on the Pi at `http://127.0.0.1:18791/` and fresh `~/logs/openclaw-gateway.err.log` stayed empty during a post-fix health check.
- Bootstrapped the Pi OpenClaw sub-workspaces (`workspace-finance`, `workspace-land-scout`, `workspace-pr`, `workspace-moderator`, `workspace-governance`) with concrete `IDENTITY.md`, `USER.md`, `MISSION.md`, `CONSTRAINTS.md`, `MEMORY.md`, and `memory/2026-03-11.md` + `memory/2026-03-12.md`; removed stale `BOOTSTRAP.md` files.
- Added `memory/latest-governance.md` plus analogous latest-summary files for the other Pi role workspaces so autonomous sessions have real local context.
- Cleaned live Uberspace bot env in `~/robinhoodcoin-freeland/.env`: deduplicated `OPENCLAW_DAEMON_ENABLED`, set `OPENCLAW_DAEMON_ENABLED=false`, set `OPENCLAW_TIMEOUT_MS=420000`, and set `LAND_SCOUT_AVOID_SOURCE_HOSTS=landwatch.com,land.com,landsearch.com,landandfarm.com,propiedades.com`.
- Restarted `robinhoodcoin-bot` on Uberspace and verified the running process environment now includes `OPENCLAW_TIMEOUT_MS=420000` and the expanded `LAND_SCOUT_AVOID_SOURCE_HOSTS` list.
- Verified end-to-end Uberspace -> Pi gateway connectivity post-fix with an authenticated chat completion request that returned HTTP `200` and assistant content `ok`.
- Rotated noisy live error logs with backups to reduce stale error visibility:
  - Pi: archived and truncated `~/logs/openclaw-gateway.err.log` and `~/logs/openclaw-tunnel.err.log` into `~/logs/archive/`.
  - Uberspace: archived and truncated `~/logs/robinhoodcoin/bot.err.log` and `~/logs/robinhoodcoin/autonomy.err.log` into `~/logs/robinhoodcoin/archive/`.
- Post-rotation checks: fresh Pi gateway stderr was empty and fresh Uberspace `bot.err.log` remained empty while `robinhoodcoin-bot` stayed running.
- Added a Pi browser watchdog in `scripts/raspberrypi/openclaw-browser-watchdog.py` plus installer `scripts/raspberrypi/install-openclaw-browser-watchdog.sh` to cap OpenClaw browser tabs and shut down idle blank sessions.
- Installed the watchdog live on the Pi as `openclaw-browser-watchdog.timer` with `OPENCLAW_BROWSER_MAX_TABS=2`, `OPENCLAW_BROWSER_MAX_AGE_SECONDS=900`, `OPENCLAW_BROWSER_IDLE_STOP_SECONDS=300`, and a 60-second cadence via `~/.config/openclaw-browser-watchdog/env`.
- Verified live behavior on the Pi: with five tabs open, the watchdog trimmed back to the two newest real pages; in a separate test with a temporary 5-second idle threshold, it stopped the browser once only a blank tab remained and aged past the threshold.
- Committed the full local worktree on branch `codex/develop` as `95a6b3c` (`Ship launch updates and browser watchdog`) and pushed it to `origin/codex/develop`.
- Deployed the current checkout to Uberspace with `deploy/uberspace/deploy-local.sh` targeting `eggman3@lynx.uberspace.de`; local backend build, static Vite build, and Next.js build all completed successfully during deploy.
- Post-deploy checks passed: `supervisorctl status` shows `robinhoodcoin-bot` and `robinhoodcoin-web-next` running, and `https://freeland.rocks/` returns HTTP 200 serving the new homepage with the campaign waitlist.
