## 2026-03-07

- Verified `freeland.rocks` was already serving the Next.js homepage with the gradient hero / stats / mission sections before new changes.
- Patched `apps/web-next/prisma/import-from-json.ts` to support both flat candidate arrays and `docs/EU_PARCEL_TABLE_V1.json` scout-table payloads, with normalized title/location/country/status fields.
- Patched `apps/web-next/app/page.tsx` so homepage stats show `Tracked Leads` and `Lowest Known Price` instead of coupling price to the top-score record.
- Synced the patched Next app files to Uberspace, rebuilt `apps/web-next`, imported the EU scout JSON into the live Prisma DB, and verified `https://freeland.rocks/api/parcels` now returns 6 rows total (2 seeded US leads + 4 EU scout leads).
- Recovered `robinhoodcoin-web-next` supervisor health by clearing two orphaned `next-server` processes on port 3200; service is now running again under supervisor.
- Verified the daily 09:00 project email is not actually scheduled: no Codex automation exists, no Uberspace crontab exists, and `notify:project` is not wired to any scheduler. Only an autonomy cycle around 09:00 on 2026-03-05 was found, and it aborted.
