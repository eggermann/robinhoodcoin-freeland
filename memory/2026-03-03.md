## 2026-03-03

- Pi OpenClaw: enabled model allowlist entry `nvidia/moonshotai/kimi-k2.5` with alias `nvidia-kimi` in `~/.openclaw/openclaw.json`; added `models.providers.nvidia` (OpenAI-compatible) pointing at `https://integrate.api.nvidia.com/v1`.
- Pi systemd: added drop-in `~/.config/systemd/user/openclaw-gateway.service.d/env.conf` to load `EnvironmentFile=-%h/.openclaw/.env` so provider keys can be injected via env file.
- Copied helper scripts to Pi repo: `~/robinhoodcoin-freeland/scripts/raspberrypi/enable-openclaw-nvidia-kimi.sh` and `enable-openclaw-openai-gpt-4-1-mini.sh`.
- Gateway recovered and listens on `127.0.0.1:18789` again after restart loops.
- Synced `NVIDIA_API_KEY` from `~/robinhoodcoin-freeland/.env` into `~/.openclaw/.env` and restarted `openclaw-gateway` so the process env has the key.
