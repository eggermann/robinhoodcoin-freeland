## 2026-03-05

- Implemented Telegram-safe plain-text sanitation + chunking for OpenClaw outputs and /ask replies; removed Markdown parse_mode in OpenClaw reply paths.
- Enabled OpenClaw browser on Pi (`browser.enabled=true`, `headless=true`, `noSandbox=true`) and restarted `openclaw-gateway`; confirmed listeners on 18789 and 18791 and browser control endpoint responds 401 (alive/auth required).
- Updated Uberspace `.env` to `OPENCLAW_TIMEOUT_MS=180000` and restarted `robinhoodcoin-bot` via supervisor; bot running with new PID.
- Direct Uberspace completion smoke test still can run long and timed out at 140s in one run, indicating remaining latency/tool-behavior risk beyond previous 60s cap.
- Added structured ask failure logging (`data/bot/ask-failures.jsonl`) with reference IDs; Telegram timeout/error replies now include `Ref: ask-...` for traceability.
- Deployed updated `dist` to Uberspace and restarted `robinhoodcoin-bot`.
- Diagnosed ask ref `ask-mmd9qkew-lw3bub`: both runtime models timed out at 180s due overloaded OpenClaw main lane queue.
- Added URL health fast-path in `/ask` for short URL/status-style messages (direct fetch + HTTP/title response) to bypass OpenClaw queue.
- Removed `freeland` keyword from land-scout role routing to avoid over-routing normal freeland.rocks links into heavy land-scout path.
- Deployed updated dist to Uberspace and restarted `robinhoodcoin-bot`.
- Added URL probe logging to `data/bot/url-probes.jsonl` and deployed; short URL status messages now bypass OpenClaw and are logged with HTTP/final URL/title.
