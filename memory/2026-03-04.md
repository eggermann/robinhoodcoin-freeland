## 2026-03-04

- Pi remote desktop: switched from tiny TFT-mirrored X11-only workflow to Virtual VNC daemon for larger remote desktop sessions.
- Enabled and started `vncserver-virtuald.service` on the Pi (listening on TCP 5999).
- Set `/etc/vnc/config.custom` to `Authentication=VncAuth`, `SecurityTypes=VncAuth`, `Encryption=PreferOff`, `ConnectToExisting=1`, and reused existing service-mode VNC password hash.
- Set `/home/pi/.vnc/config` for user `pi` to `-geometry 1280x720` and `-depth 24`.
- Verified with `vncserver-virtual -test :1` that Xvnc command resolves to `-geometry 1280x720 -depth 24`.
- Remote desktop usability fix: disabled `vncserver-x11-serviced` (tiny TFT mirror) and forced `vncserver-virtuald` to own port 5900 via systemd override (`/etc/systemd/system/vncserver-virtuald.service.d/override.conf`).
- Current VNC endpoint `192.168.1.17:5900` now points to Virtual Mode (not TFT mirror), with VncAuth password auth and `ConnectToExisting=1`.
- Kept user virtual desktop defaults in `/home/pi/.vnc/config` at `1366x768`, depth 24.
- VNC auth fix: switched `vncserver-virtuald` from `VncAuth` to `SystemAuth` due repeated `NoAuthMethods` negotiation failures and client blacklisting.
- `vncserver-virtuald` now runs on port 5900 with `ConnectToExisting=1`; login uses Linux credentials (`pi` + SSH password).
- RealVNC Virtual Mode is license-blocked on this Pi (`ServerLaunchFailed` with required license), so switched to a free alternative.
- Installed `tigervnc-standalone-server` and configured user service `~/.config/systemd/user/tigervnc.service`.
- TigerVNC now runs a virtual desktop on `:1` / TCP `5901` (1366x768, depth 24) using LXDE startup script.
- Disabled both RealVNC daemons (`vncserver-virtuald`, `vncserver-x11-serviced`) to avoid 5900 confusion.
- Temporary TigerVNC password initialized as `Freeland5901` (should be changed immediately with `tigervncpasswd`).
- TigerVNC compatibility tweak: set `~/.config/tigervnc/config` to `securitytypes=VncAuth` only (removed TLSVnc/VeNCrypt) for better compatibility with RealVNC Viewer.
- OpenClaw Control UI auth fix: discovered Control UI requires browser-local token storage (`openclaw.control.settings.v1`) even when gateway service has token configured.
- Ran tokenized dashboard open on Pi display (`DISPLAY=:0`) to seed browser session, and synced `OPENCLAW_GATEWAY_TOKEN` in `~/robinhoodcoin-freeland/.env` from `~/.openclaw/openclaw.json` for bot-side gateway auth consistency.
